RewriteEngine On

# Unless directory, remove trailing slash
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([^/]+)/$ /$1 [R=301,L]

# Redirect external .php requests to extensionless URL
RewriteCond %{THE_REQUEST} ^(.+)\.php([#?][^\ ]*)?\ HTTP/
RewriteRule ^(.+)\.php$ /$1 [R=301,L]

# Resolve .php file for extensionless PHP URLs
RewriteRule ^([^/.]+)$ $1.php [L]

# RewriteRule ^([^/.]+)/([^/.]+)$ $1.php?urun=$2 [L]
# ürün ve hesap sayfası için gerekli şeyleri bu şablondan çıkardım

# urun.php sayfası için
RewriteRule ^(urun)/([^/.]+)$ urun.php?urun=$2 [L]

# RewriteRule ^([0-9a-zA-Z-_]+)/([0-9]+)$  posts.php?sef=$1&id=$2 [L,QSA]

# dosyaların sonunda .php olmadığı için dizinleri dosya zannedip not found fırlatıyor,
# dizinlerin içine php dosyası koymak istiyorsan bunun bir çaresini bulman gerekli